#pragma once

// 最大速度
static const float kMaxTimeScale = 2.0f;
// 最低速度
static const float kMinTimeScale = 0.0f;

// オブジェクトの最大存在数
static const int kMaxAliveObjects = 32;

// プレイヤーの発射間隔
static const float kFireInterval = 10.0f;
// プレイヤーの弾速度
static const float kBulletSpeed = 1.0f;
// プレイヤーの弾の存在する時間
static const float kLifeTime = 60.0f;

// 敵の発射間隔
static const float kEnemyFireInterval = 60.0f;

// 敵の回転速度
static const float kEnemyRotateSpeed = 0.1f;

// パーティクルの最大存在数
static const int kMaxAliveParticle = 32;

// スプライトの最大存在数
static const int kMaxSpriteCount = 32;